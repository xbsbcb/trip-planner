[project]
name = "trip-planner"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "langchain-deepseek>=0.1",
    "pydantic>=2",
    "pydantic-settings>=2",
    "python-dotenv>=1",
]
